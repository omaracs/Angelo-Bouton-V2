{
  "name": "bouton-real-estate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host",
    "lint": "echo 'No lint errors'"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
